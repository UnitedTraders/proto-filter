syntax = "proto3";

package nested.orders;

message Order {
  string id = 1;
  string product = 2;
  int32 quantity = 3;
}

service OrderService {
  rpc GetOrder(GetOrderRequest) returns (Order);
}

message GetOrderRequest {
  string id = 1;
}
